{
  "name": "myapp",
  "description": "a really cool app",
  "version": "1.0.0",
  "engines": {
    "node": "6.11.1"
  },
  "dependencies": {
    "client": "file:client",
    "server": "file:server"
  },
  "scripts": {
    "postinstall": "cd client && ng build --prod",
    "start": "node server/index.js"
  }
}